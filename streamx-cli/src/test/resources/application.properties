streamx.cli.settings.root-dir=./target/test-classes/dev.streamx.cli.settings
propertySource=classPathProperty
streamx.dev.dashboard.open-on-startup=false

# stdout is written to console by default, so writing same to logs is redundant
%test.quarkus.log.category."dev.streamx.cli.util.Output".level=OFF

# Used by interpolation tests
string.array.property=stringArrayValueFromFile
string.property=stringValueFromFile
integer.property=1
